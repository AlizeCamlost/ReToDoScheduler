# Web Client

The mac/web frontend lives here.

## Dev server

Run the Vite dev server:

```bash
npm --prefix apps/web run dev
```

By default this starts a local frontend server (typically `http://localhost:5173`).
